global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'aitudesk-backend'
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:4000']
        labels:
          service: 'aitudesk-api'
